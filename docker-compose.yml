services:
  wrong-notebook:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wrong-notebook
    restart: always
    ports:
      - "3001:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./config:/app/config
