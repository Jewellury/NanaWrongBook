# AI 执行摩擦修复方案

> 修复范围：CLAUDE.md + plan.md + execute.md + audit.md（共 4 个文件）
> 7 个摩擦点全部修，分两个文件写变更清单

---

## 一、CLAUDE.md 修复清单（5 处变更）

### 变更 1：文件头加优先级声明（修摩擦 3）

在文件最开头（第 1 行之前）插入：

```
<!-- 
  规则优先级（从高到低）：
  安全铁律 > 三代理门禁 > Git 协作规范 > 上下文保护规则
  当规则互相冲突时，高优先级覆盖低优先级。
-->
```

### 变更 2：上半 context-mode 中文化（修摩擦 2）

L1-62 的英文 context-mode 规则改为中文。不删内容，只翻译。第一行标题改为：

```
# 上下文保护规则（给 AI 看的技术规则，用户无需关注）
```

保留所有技术细节，只是从英文变中文。

### 变更 3：删/改 500 字限制（修摩擦 1）

原文 L52 `Keep responses under 500 words.` 改为：

```
- 日常对话回复简洁（不超过 500 字）
- /plan /execute /audit 产出的计划、执行日志、审计报告不受字数限制
- 代码和配置写入文件，不堆在对话里
```

### 变更 4：三代理部分精简为骨架（修摩擦 4）

原文 L66-91（"三代理协作开发规范"整节）替换为：

```
# 三代理协作开发规范（骨架）

本项目采用「/plan → /execute → /audit」三代理闭环。完整规则在命令文件中：
- .claude/commands/plan.md
- .claude/commands/execute.md
- .claude/commands/audit.md

## 文档路径速查

| 文件 | 用途 | 规则 |
|------|------|------|
| doc/spec/ | 技术规格 | 长期保留 |
| doc/plan/ | 实施计划 | 长期保留，每轮一份 |
| doc/executionlog/ | 执行日志 | 长期保留，每轮一份 |
| doc/auditlog/ | 审计报告 | 长期保留，每轮一份 |
| doc/progress.md | 项目全程历史 | 只增不减 |
| doc/active_spec.md | 当前活跃任务 | 每轮替换 |

## 门禁摘要（详情见命令文件）

1. /plan 未经你确认 → /execute 不得启动
2. /execute 不可偏离计划（小调整可记录后继续，大偏离回 /plan）
3. /audit 只指出问题、不直接改代码
4. 审计通过 → 更新 progress.md + active_spec.md
```

`doc/progress.md` 和 `doc/active_spec.md` 的更新规则也统一归到这里，命令文件里不再各自写。

> 安全铁律那节（L95-129）不动，保持原样。
> Git 协作规范那节（L133-173）不动，保持原样。

### 变更 5：Git 协作规范保留不删

安全铁律和 Git 协作规范这两节是 CLAUDE.md 独有的（命令文件里没有），不需要去重，保留不动。

---

## 二、plan.md 修复清单（1 处变更）

### 变更：明确"写代码"边界（修摩擦 7）

原文 L11-14 "禁止写代码" 那段替换为：

```
1. **禁止修改源码文件**：你不修改 src/、prisma/、lib/ 等源码目录中的任何文件。
   但计划文档中可以写伪代码、Prisma schema 草稿、API 路由签名、函数签名——
   这些是设计表达，不算"写代码"。
2. **禁止跳过确认**：计划写完后，必须等用户说"确认"或"开始"才能进入执行阶段
3. **大白话输出**：计划文档的核心部分（要做什么、怎么做、怎么验证）必须用大白话写，
   让非技术用户能看懂。技术细节放在独立的"技术附录"节中
```

同时把模板中的"6. 技术附录（可选）"改为"6. 技术附录"，去掉"可选"——鼓励把技术细节放进去。

另外增加一行参考源：

```
- 你参考：doc/spec/ 中的技术规格文档、doc/progress.md 中的项目历史、doc/active_spec.md 中的当前活跃任务
```

---

## 三、execute.md 修复清单（1 处变更）

### 变更：区分重大偏离和微调（修摩擦 5）

原文 L12-13 "不可偏离计划" 替换为：

```
2. **偏离计划分两级处理**：
   - 不影响验收标准/任务数量/核心逻辑的小调整（如函数命名优化、边界条件补充）：
     在执行日志中记录即可，不中断流程
   - 影响验收标准、文件变更清单、或任务增减的大偏离：
     停下来报告用户，回到 /plan 修订计划后再继续
```

---

## 四、audit.md 修复清单（1 处变更）

### 变更：测试检查改为"如有则跑"（修摩擦 6）

原文 L73（审计工作流第 3 步）替换为：

```
3. 运行测试：如项目中存在与本次变更相关的测试（npm test 或 jest 相关文件），
   则运行并报告结果；如不存在，跳过此步，在"用户验证指南"中列出建议的手动验证步骤
```

同时审计报告模板中"测试"那节改为：

```
### 测试
- [ ] 相关自动化测试已运行并通过（如存在）
- [ ] 如无自动化测试，手动验证步骤已列出（见"用户验证指南"）
```

---

## 五、变更文件汇总

| 文件 | 变更数 | 修了哪个摩擦 |
|------|:--:|------|
| CLAUDE.md | 5 处 | #1 #2 #3 #4 |
| .claude/commands/plan.md | 1 处 | #7 |
| .claude/commands/execute.md | 1 处 | #5 |
| .claude/commands/audit.md | 2 处 | #6 |

共修改 4 个文件、9 处变更。

---

## 六、确认后执行

以上所有变更的具体文字（old → new）已在上文中逐段给出。
确认后我逐文件修改，改完提交 dev 并推送。
